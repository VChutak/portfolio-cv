---
import "../styles/global.css";
const { title, subtitle } = Astro.props;
---

<div class="max-w-7xl mx-auto flex flex-col gap-4 px-4 sm:py-10 py-6">
  <h2 class="text-4xl font-bold border-l-4 pl-4">{title}</h2>
  {subtitle && <p class="max-w-[70ch]">{subtitle}</p>}
</div>
